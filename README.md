# ChornoGraph
ChronoGraph is a local-first project scheduling app that models tasks as a dependency graph, computes the critical path, runs Monte Carlo delivery simulations, and helps teams analyze schedule risk directly in the browser.
